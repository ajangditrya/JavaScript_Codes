// Introduction to JavaScript

console.log("Hello World")          //no need to put ; in the end, but if added, no difference

function addthreenumbers(a,b,c){    //fxn in javascript
    return a+b+c;
}

let c=addthreenumbers(1,2,3)        //calling the fxn, storing the return value;
console.log(c);